name: Build and Push Posts Service

on:
  pull_request:
    paths:
      - 'blog/posts/**'
  workflow_dispatch: {}

jobs:
  build-push-posts:
    runs-on: ubuntu-latest
    env:
      AWS_REGION: ${{ secrets.AWS_REGION }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Timestamp
        id: vars
        run: echo "timestamp=$(date -u +%Y%m%d-%H%M)" >> $GITHUB_ENV

      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.ECR_POSTS_URI }} ./blog/posts
          docker tag ${{ secrets.ECR_POSTS_URI }}:latest ${{ secrets.ECR_POSTS_URI }}:${{ env.timestamp }}

      - name: Push Docker Image
        run: |
          docker push ${{ secrets.ECR_POSTS_URI }}:${{ env.timestamp }}
